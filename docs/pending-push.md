# Pending git push

When you have stable internet, run:

```bash
cd /Users/chrisanderson/streamwise && git push origin main
```

**Why:** `main` was ahead of `origin/main` after Roku Streaming Day promos (`d519e13`) failed to push (`Could not resolve host: github.com`).

Delete this file after a successful push if you like.
