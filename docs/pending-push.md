# Pending git push

When you have stable internet, run:

```bash
cd /Users/chrisanderson/streamwise && git push origin main
```

**Why:** `main` is **2 commits ahead** of `origin/main` (push keeps failing: `Could not resolve host: github.com`):

1. `d519e13` — Roku Streaming Day promos  
2. `9c5a93f` — DirecTV Stream trial + intro promos

Delete this file after a successful push if you like.
